# Build-time defaults. Both can be overridden at runtime from the popup's
# Settings, so the committed dist/ carries neither and a trader points it at
# their server themselves.
VITE_API_BASE=http://localhost:8000
VITE_SITE_URL=http://localhost:3000
