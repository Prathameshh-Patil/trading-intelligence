# Where services/api lives. Required for the licence check; also points the
# forecast table at GET /api/v1/forecast/table instead of the committed fixture.
VITE_API_BASE=http://localhost:8000
# The customer site, for the "Open my account page" link on the licence screen.
VITE_SITE_URL=http://localhost:3000
# VITE_ENGINE=mock|real, VITE_CONTEXT=mock|real -- see src/lib/engine/index.ts
