# Daily Update — 2026-09-18

**Branch:** `main` (via `feat/magnitude-expansion`, fast-forwarded)
**Reference:** [`docs/superpowers/specs/2026-09-18-magnitude-expansion-design.md`](../docs/superpowers/specs/2026-09-18-magnitude-expansion-design.md) ·
[`docs/superpowers/plans/2026-09-18-magnitude-expansion.md`](../docs/superpowers/plans/2026-09-18-magnitude-expansion.md) ·
[`plans/team/strategy-precommit.md`](../plans/team/strategy-precommit.md) §13–§17

---

## 1. Summary

**Day 1 of the spot-XAUUSD block opened at 00:00 and ran to 01:00.** Six commits, all by Claude at
Varad's direction, merged to `main` as a fast-forward (`5092450..0841fed`). **381 tests passing, up
from 337**; `ruff` clean; `mypy` clean across 64 source files, up from 58.

| | Task | State |
| :--- | :--- | :--- |
| 0 | S13 seam frozen, §13–§17 structured | ✅ code done, **§6 blocked** |
| 1 | E0 — S3 transport | ⛔ **blocked on AWS credentials**; offline half built |
| 4 | E3 — the cost model | ✅ arithmetic half; **empirical half blocked** |
| 6 | Yang-Zhang | ✅ |
| 9 | Hurst — DFA and variance-time | ✅ |
| 2, 3, 12 | E1, E2, E4 | ⛔ **blocked on four unwritten predictions** |
| 7 | GARCH(1,1)-t | ⛔ blocked on the `scipy` dependency call |

**Nothing was measured against the strategy today.** No backtest, no fold, no result. Everything
below is infrastructure, and the four experiments that could produce a finding are all gated.

## 2. What is blocked, and on whom

| | Item | Whose | Why it cannot move from here |
| :--- | :--- | :--- | :--- |
| §14 | E1's prediction | Varad | A prediction written after seeing the distribution is not a prediction. Drafts exist ([§6](#6-drafted-predictions-and-why-they-are-in-a-separate-file)); the signature does not |
| §15 | E2's prediction | ~~Prathamesh~~ **Varad**, delegated ~11:00 | Same. Prathamesh handed §15 to Varad: stages 3–7 are the magnitude axis and the draft argues stage 3 does the killing, so the owner of that model writes the number. The 00:15 draft stands as the thing to sign or replace |
| §16 | E3's prediction | Varad | Same. The arithmetic half needed none and is built |
| §17 | E4's prediction | Varad | Same |
| E0 | AWS account for requester-pays | Varad | No `aws` CLI and no `~/.aws` on this machine. `roles.md` puts spend decisions with Varad against the $280–300 ceiling. Exposure is ~$0.06 by Dukascopy's own worked example |
| T7 | `scipy` into the lockfile | Varad | A lockfile change earns its own reviewed commit in this repo |

**Suspended for the block and recorded as missed, per `gates.md`'s rule:** the MT5 desktop install,
R7's depth check, and the `vitest` add. All three keep their Mon 21 Sep slot. 🔑 **#0, the Anthropic
key, was NOT suspended** and remains due today.

## 3. The seam, and the two rules it enforces by test

`features/frame.py` — **S13**, frozen before either lane wrote a feature, registered in
[`contracts.md`](../plans/team/contracts.md) beside S8 and S9. `FEATURES` is the exact column set
and `require()` rejects **missing and extra** columns, naming the offender in both cases.

Two rules are machine-checked rather than reviewed:

- **No column is named in ticks or pips.** Tested both on what passes through the seam *and* on
  `FEATURES` itself, so the rule survives whoever next edits the tuple.
- **A column is never dropped to signal a dead feature.** If E4 retires the HMM, `p_build` and
  `p_expand` stay and carry NaN. Removing a column is a seam change and needs both lanes.

⚠️ **OFI, CVD and Hawkes intensity are absent rather than present-and-NaN.** `XAUUSD.has_flow` is
`False` and `require_flow` raises. Absent means a lane cannot quietly begin writing an
indicative-volume proxy into them — the ruling `spot.py` and `Quant_Trading`'s tick fetcher reached
independently.

## 4. Three corrections to the plan, all found by building it

**These are corrections to work written yesterday, not to the repo.** Recorded because two of them
would have produced plausible wrong numbers rather than failures.

### ⚠️ The plan's Yang-Zhang constant was wrong

It asserted `_yz_k(12) == 0.20880`. The correct value is **0.134823**:

```
n=     2  k=0.078341
n=    12  k=0.134823
n=    48  k=0.142704
n=   288  k=0.144868
n= 10000  k=0.145287     -> 0.34/2.34, the large-n limit
```

`k` never leaves `[0.078, 0.146]`, so 0.209 was never reachable. A second test pins that band, so a
mistyped formula fails rather than returning a plausible sigma.

### ⚠️ The window boundary was off by one, and the extra bar is the estimator's actual shape

First valid index is **`n`, not `n−1`**. The overnight term is `ln(O_t / C_{t−1})`, so `n` overnight
returns need `n+1` bars. An implementation answering at index 11 for `n=12` would be averaging
eleven returns and a NaN — a smaller number that reads as calm, and `r_ratio` divides by it.

### ⚠️ `D` is exempt from §7.3's monotonicity rule, and the exemption is arithmetic

Computed from `e3_cost.surface` at the archive's 1.91 bp:

```
D      $2.00   $2.60   $3.00   $4.00   $5.00
EV bp   7.03    6.01    5.33    3.62    5.00      <- minimum at $4.00, then rises
```

**§10's `R = max(2.5D, $10.00)` is the cause.** Below `$4.00` the target *floor* binds, so the
payoff ratio slides 5:1 → 2.5:1 as `D` widens and EV falls; past `$4.00` the multiple takes over and
EV climbs. The dip is the spec's own target formula, and §7.3 as written would have rejected `D`
for fitting noise. **The general rule, now in the spec: the monotonicity check assumes a parameter
enters EV once, and `D` enters twice** — as the loss leg and, through the target formula, as the win
leg. `R`'s multiple has the same property and needs the same look.

## 5. Two guards found by testing rather than by review

### 🔢 The cost model's factor of ten, pinned against two independently published numbers

`to_bp` is one division, and a draft of the design got it wrong by 10× — `$0.30` on `$3,400` read as
8.8 bp instead of **0.88** — which flipped expectancy at the spec's own parameters from +6 bp to
−2 bp and reversed the viability call. **A one-line function that can reverse a decision earns tests
against known values**, so it now carries two: one GC tick is `$0.10`, and `M3B_DRIFT.md` prices the
GC round trip at 0.3504 bp including commission, so 0.294 bp of spread must come back.

Corrected, the three venues are **GC 0.29 bp · prop firm 0.88 bp · Dukascopy 1.91 bp**, every
break-even lands at **0.19–0.38** against the spec's claimed 42%, and **the binding constraint is E1
— whether gold makes the move at all — not cost.**

### 📉 DFA needs a minimum scale of 4, and without it returns H = 14

A least-squares line through 2 or 3 points fits them almost exactly, so `F(s)` collapses toward
zero, `log F(s)` toward `−inf`, and the slope explodes. **Measured before the guard: including
`s=2` returned H = 14.0 on a series whose true H is below 0.5.** Variance-time needs no such floor
because it fits no trend. Both estimators now refuse on fewer than three usable scales — a slope
through two points is exact and meaningless.

### 📏 H is scale-dependent, and that constrains the data E0 has to deliver

The mean-reversion test measures at scales 4–32 rather than 4–256, and **the restriction is the
finding rather than a way to make it pass.** AR(1) anti-persistence is short-range; by `s=128` the
correlation has decayed and the series is a random walk again, so a pooled regression is dominated
by the long end:

```
phi = -0.6, same series    scales 4..32     dfa 0.288   vt 0.444
                           scales 4..256    dfa 0.373   vt 0.476
```

Correct, and blind to what the series contains. This is why §6 asks for separate readings at 5m and
15m rather than one pooled number.

⚠️ **The consequence the plan did not state: `h_dfa_15m` cannot be computed from 5-minute bars.**
Three bars is not three scales. §6 needs 1-minute bars or ticks, and `spot.load_bars` currently
resamples to 5min. **This lands on E0** — the granularity pulled has to support the Hurst the S13
frame declares. Flagged, not decided.

Also pinned: both estimators take **log price levels**, and passing returns shifts H by more than
0.3 — roughly the entire usable range, so the mistake is otherwise silent.

## 6. Drafted predictions, and why they are in a separate file

Varad asked for drafts to edit. They are in
[`plans/team/strategy-precommit-drafts.md`](../plans/team/strategy-precommit-drafts.md) and
**deliberately not in `strategy-precommit.md`**, so that file only ever holds text someone signed.
**Editing a block and moving it across is the signature.** Every number derives from results already
published here — §2's `atr_bp` quartiles, `SPOT_FEED_CHECK.md` §1, `regimes_2026-09-02/README.md` —
not from running the experiments, and each draft ends with the three places it is most likely wrong.

**Three of the four predict their own kill condition fires.** The strongest is E4's, and it is the
one worth reading first: `regimes_2026-09-02` fit `k=3` on this instrument with two CVD features,
and **regimes 0 and 1 came back identical on every feature except the *sign* of `cvd_slope`**, with
`realized_vol` spanning only 0.000275–0.000357 across all three states. The states resolved on flow,
not volatility. Spot removes the tape, so the separating variable is gone.

## 7. The Dukascopy wall, re-measured again

Yesterday's §12 recorded the 429. Two further measurements today:

```
candle endpoint, 12 consecutive, 00:0x     0 ok / 12   HTTP 429
candle endpoint, single probe, 00:4x       HTTP 429     <- ~40 min later, unchanged
```

**The limit did not lift in 40 minutes**, so it is not a pacing problem and no retry schedule
reaches it. The S3 requester-pays route remains the plan, and `spot_s3.py` is written so that E0 is
one `aws s3 ls` once credentials exist.

⚠️ **`OBJECT_SUFFIX` is the documentation's claim, not a measurement.** Nobody has listed the
bucket. `day_prefix` is tested — including against `dukascopy.url_for`, so the two layouts cannot
drift — and `fetch_day` deliberately is not. **Finding the real object name is E0's first action.**

## 8. What changed on disk

```
services/signal-data/features/frame.py          new   S13
services/signal-data/features/expansion.py      +     yang_zhang, hurst_dfa, hurst_vt, hurst_agree
services/signal-data/e3_cost.py                 new   the cost model
services/signal-data/spot_s3.py                 new   transport, offline half
services/signal-data/tests/test_frame.py        new   7 tests
services/signal-data/tests/test_e3_cost.py      new   12 tests
services/signal-data/tests/test_spot_s3.py      new   6 tests
services/signal-data/tests/test_features.py     +     19 tests
plans/team/contracts.md                         +     S13
plans/team/strategy-precommit.md                +     §13-§17, four predictions unwritten
plans/team/strategy-precommit-drafts.md         new   unsigned drafts
docs/superpowers/specs/...-design.md            ~     §7.3's D exemption
```

```
381 passed in 9.66s
All checks passed!
Success: no issues found in 64 source files
```

Commits `c4f879b` · `c658c19` · `629ba06` · `b6116d1` · `00fc3c2` · `0841fed`, merged
`5092450..0841fed`.

## 9. What Friday needs from a person

In the order that unblocks the most:

1. **The four predictions.** They gate E1, E2, E4 and E3's empirical half — four of the five
   experiments. Drafts are ready to argue with.
2. **An AWS account.** E0 is the plan's critical path and every Day-2 data task sits behind it.
3. **The `scipy` call**, which unblocks GARCH and therefore the ensemble.
4. **🔑 The Anthropic key** — ten minutes, and it was explicitly not suspended.

**And one decision the Hurst work forces:** §6 wants a 15-minute Hurst, which needs 1-minute bars or
finer. If E0 pulls 5-minute bars only, `h_dfa_15m` and `h_vt_15m` cannot be computed and two S13
columns are NaN by construction. **That is a choice about what E0 pulls, and it should be made
before the pull rather than discovered after it.**

---

# Second session — 01:00 to 09:14

> §1–§9 above close at 01:00. **This is the same calendar day and the same Day 1 of the block** —
> Day 2 is Saturday 19 Sep and has not started. Five more commits, and **every task in the
> three-day plan that did not need a person is now built.**

## 10. Task 14 — the tuning harness, and the number that makes §7.3 load-bearing

`tuning.py`. Pure index arithmetic plus `statistics.NormalDist`, so **no data, no predictions and
no new dependency** — which is why it was the one substantial thing left that nobody had to unblock.

**§14 asks for 24/6/roll-3 and warns against random splits because they "leak time-series
information". A rolling split leaks too**, and the leak it does not mention is the larger one here:
a 90-minute label opened at 15:00 on the last training day resolves inside the validation fold.
Purging closes it; nothing in §14 asks for purging.

**Both guards are mutation-tested rather than asserted.** One test checks the purge holds; a second
checks that *removing* it would admit those samples, so the first cannot pass for the wrong reason.
Same pair for the embargo.

`Trials` has `__slots__`, a read-only count, no reset and no decrement, and a test asserts no such
method exists. `haircut_report` takes the counter rather than an `int`, so a caller cannot pass a
number it invented without first constructing a counter that recorded them.

**The haircut does visible work at this sample size:**

```
  K    null SR   DSR at SR=0.10
  1    0.0403       0.908
 20    0.0851       0.630
 60    0.1050       0.456
200    0.1238       0.298
```

⚠️ **Sixty configurations is enough to make a per-trade Sharpe of 0.10 worse than a coin flip at 500
observations.** §7.3's three-tuned-parameters-per-fold cap is doing real work, not being cautious —
and if E2 returns the low end of its drafted 200–500 trades, the budget is tighter still.

## 11. Two bugs in that harness, found hours later by running it at the spec's own defaults

**Neither was caught by reading the code.** Both came from exercising the functions at §14's stated
parameters rather than at the values the tests happened to pick.

⚠️ **A test that asserted no-overlap on parameters chosen to produce none.** It passed `roll_m=6`
with `val_m=6` and so never exercised the defaults of roll 3 / window 6. At those defaults **every**
consecutive pair shares three months:

```
0: val 2022-01-01 .. 2022-06-30
1: val 2022-04-01 .. 2022-09-30      <- three months of fold 0
2: val 2022-07-01 .. 2022-12-31      <- three months of fold 1
```

The overlap is not a defect — §14 specifies that roll. **The defect was a test implying it does not
happen**, and the consequence is easy to miss: ten folds are not ten independent observations, a
good month is scored twice and reads as two confirmations, and `deflated_sharpe` treats trials as
independent. `independent_folds()` now makes the discount countable: **ten folds at §14's defaults
are five independent windows.**

⚠️ **A haircut present in no theory.** `expected_max_sharpe(n_trials=1)` returned 0.0403. The
expected maximum of *one* standard normal is 0, so at K=1 the deflated Sharpe reduces to the
probabilistic Sharpe against a zero benchmark. The special case had been invented to satisfy a test
that passes without it.

## 12. A code review at high effort, and all eight findings were real

Run over `5092450..HEAD`. **Every finding was verified before being fixed rather than taken on
faith**, and both mediums reproduced with a measurement.

🔴 **The consequential one.** `hurst_dfa` applied a scale ≥ 4 floor and `hurst_vt` did not, so a
shared `scales=(2,4,8,16,32)` had DFA regressing over four scales and VT over five. **H is
scale-dependent, so `hurst_agree` was comparing two different quantities.** Measured on a pure
random walk: `dfa 0.563, vt 0.510, agree False` — **the gate closing on the one series whose answer
is known to be 0.5.** The floor moved into `_usable_scales` so both share it.

⚠️ **And fixing it uncovered a second problem.** Sharing a scale set is necessary and not
sufficient. Restricted to `(4,8,16,32)`, DFA reads **0.563** against variance-time's **0.513** on a
true 0.5 — DFA's finite-size bias at small windows. The gap is 0.050 against an `AGREE_TOL` of
**0.05**, so agreement still fails on a short ladder. **Pinned as its own test rather than tuned
around:** widening the tolerance would hide the bias instead of measuring it.

🔑 **That is now a SECOND, independent reason §6's 15-minute Hurst cannot come from coarse bars** —
not only too few scales, but DFA systematically biased high on the short ladder, so `h_agree` would
read False by construction. Both reasons land on the same E0 decision about granularity.

⚠️ **One finding ran in the unsafe direction.** `independent_folds` used `span // val_m`, which
counts how many windows *fit* in the span rather than how many of the actual folds are disjoint on
the `roll_m` grid. At val 6 / roll 4 it returned **7** where the true answer is **5**, verified
against a greedy reference — and a larger count is a *weaker* haircut. §14's own 3/6 divides
exactly, which is why both tests written for it passed.

**One finding reproduced live during the session:** the new `roll_m=0` test hung the suite and had
to be killed, exactly as the review predicted. `train_m`, `val_m` and `roll_m` are now validated.

The rest: `frame.require` accepted duplicated column labels (`set()` collapses them, and `df["mid"]`
then returns a DataFrame — the silent seam change the module exists to reject); zero stop distance
and zero price raised `ZeroDivisionError` rather than a named error; `_yz_k(1)` divided by zero; and
**`MONOTONICITY_EXEMPT` was exported and tested but never consulted**, so the exemption relied on
whoever wrote the tuner remembering it. `is_monotone` now takes an optional `param` and enforces it.

**Finding 7 is the one E0 must act on.** `spot_s3.fetch_day` passes a *day* anchor to an
*hour-anchored* decoder. Dukascopy's `.bi5` records carry milliseconds from the start of the **hour**,
and the HTTP layout confirms the files are hourly — so if the S3 objects are those same files,
**every tick in a day would land inside hour 0 and decode perfectly cleanly.** A frame that is wrong
and well-formed is the worst outcome available, so `fetch_day` now asserts the returned span exceeds
two hours and names the likely cause.

## 13. Tasks 10, 11 and 13 — the rest of the lane, against synthetic fixtures

**§1's windows are New York time, and that is the whole difficulty.** New York is UTC−5 for part of
the year and UTC−4 for the rest, so a UTC constant is right for half the archive and wrong by
exactly one hour for the other half — an error that reads as an off-by-one in the window logic
rather than as a timezone bug. The discriminating test: **12:00 UTC is 07:00 New York in January
(outside the morning window) and 08:00 in July (its first minute).**

Windows are half-open, or a 15:30 bar lands in two sessions and a bar counted twice is a trade
counted twice. Weekends are excluded on the **calendar**, not the clock — spot runs 24×5, so a
Saturday morning has a valid New York reading inside the window and no market behind it.

⚠️ **Recorded and not fixed: there is no exchange-holiday calendar.** Thanksgiving and Good Friday
have weekday clocks and no session, so **E2's funnel will over-count by roughly nine days a year**
until one exists. That is a direct input to E2's stage-1 number.

**§9's sweep takes the ATR leg only**, and that is a deviation worth knowing. The spec says
`max(2 ticks, 0.05 × ATR)`, but `XAUUSD.tick` is Dukascopy's 0.001 **price quantum** and
`instruments.py` says in terms it must not be used for costing. The floor is stated in USD/oz
instead — two cents, a broker increment rather than a feed's encoding precision. **If the chosen
prop firm's minimum increment differs, that constant is the one to change.**

Two behaviours pinned because either being wrong is silent: `wick_w` measures from the **extreme**
reached rather than the first tick through the level (§9's limit sits at `P_s + 0.50W`, so the wrong
origin moves every entry price), and **the 30-second rule is a rejection, not a timeout** — §9 reads
a late reclaim as *acceptance* of the level, the opposite signal, so it yields no row at all.

**§13's renormalisation is the subtle part of the FSM.** Three of its nine components died with the
tape. Feeding them `0.0` with their original weights intact would lower every score by the sum of
three weights, and `Score ≥ 0.72` would then mean something stricter than the spec wrote — **not a
high bar but an unreachable one.** They are absent from `WEIGHTS` and the survivors renormalise to
1. §13's `no_cvd_confirmation` exclusion is likewise absent rather than defaulted either way.

**§15 is built around its refusals**, each a test rather than a comment: a third trade, a second
loss, a second open position, a widened stop, a size increased after a loss, and the one most likely
to be added back under pressure — **a limit order becoming a market order on expiry.** `expire()`
returns to FLAT and there is deliberately no branch that takes the trade anyway. **The missing
branch is the rule.**

⚠️ **One defect caught before committing rather than by review.** The first `admit()` defaulted `ts`
to `0.0` behind a `type: ignore` and left `d_bp`, `r_bp` and `cost_bp` at zero — a candidate that
type-checks, reads plausibly, and carries a **1970 timestamp** into whatever consumes it. Those four
are now required arguments: a missing input stops the trade rather than being invented.

## 14. What changed on disk, second session

```
services/signal-data/tuning.py            new   purged walk-forward, Trials, the haircut
services/signal-data/features/structure.py new  §1 windows, §12 lockout, §9 sweep/reclaim
services/signal-data/fsm.py               new   §13 score, §15 state machine
services/signal-data/features/expansion.py  ~   MIN_SCALE shared by both Hurst estimators
services/signal-data/features/frame.py      ~   duplicate-label rejection
services/signal-data/e3_cost.py             ~   zero-stop and zero-price validation
services/signal-data/spot_s3.py             ~   the hour-anchor assertion
tests/test_tuning.py, test_structure.py, test_fsm.py   new
```

```
473 passed in 11.23s
All checks passed!
Success: no issues found in 70 source files
```

**337 → 473 tests across the day.** Commits `bf4795c` · `e5d128c` · `af20900` · `9a0e44b` ·
`99c66c9`.

⚠️ **`uv run` began hanging mid-session** and every command since uses `.venv/bin/python -m pytest`
directly, which runs normally. Some `uv run` processes were killed during the `roll_m=0` hang and
may have left it wedged — **worth a `uv cache clean` before relying on it.**

Also: the `walk()` test fixture is now cached. Eight tests asked for the same 20,000-step AR(1)
series and the recursion is a Python loop — `test_features.py` went from **~190s to 1.65s**.

## 15. Where the block actually stands

**Built:** Tasks 0, 4, 6, 9, 10, 11, 13, 14, plus `spot_s3`'s offline half. **That is every task in
the three-day plan that did not need a person.**

**Not built, and none of it is work — all four are decisions:**

| | Blocks | Whose |
| :--- | :--- | :--- |
| **Four predictions** | E1, E2, E4, and E3's empirical half — four of the five experiments | Varad ×3, Prathamesh ×1 |
| **An AWS account** | E0, and Tasks 5, 8, 12, 15 behind it | Varad |
| **`scipy`** | GARCH, and the ensemble behind it | Varad |
| 🔑 **The Anthropic key** | A Week-0 gate line, unmet at four gates | Varad |

**And one decision that must be made before E0 pulls anything.** §6 wants a 15-minute Hurst, and it
cannot come from 5-minute bars for **two independent reasons now** — three bars is not three scales,
and DFA is biased high on a short ladder (0.563 against a true 0.5). **If E0 pulls 5-minute bars
only, `h_dfa_15m` and `h_vt_15m` are NaN by construction and two S13 columns are dead on arrival.**
That is a granularity choice, and it is cheaper to make before the pull than to discover after it.

---

# Third session — 09:14 to 09:55

> §15 above closed with the block "entirely blocked on four decisions". **Varad authorised the
> `scipy` dependency at 09:17**, which unblocked §2's second half. Five commits, and §2 and §6 are
> now complete and — for the first time — verified against real data rather than synthetic fixtures.

## 16. GARCH(1,1)-t, and a fit that failed for a reason worth keeping

`scipy 1.18.1` landed on its own commit (`d0f7d04`), per the convention that a lockfile change
earns a reviewed commit. **Hand-rolled rather than `arch`:** the Student-t log-likelihood is ~40
lines given an optimiser, and `arch` would pull statsmodels in for one estimator. scipy earns its
place twice — `minimize` for the fit, `signal.lfilter` for the variance recursion.

**Parameter recovery is the only honest test of an estimator, and it passes:**

```
simulated   ω=2e-6   α=0.08   β=0.90   ν=6
fitted      ω=1.69e-6  α=0.069  β=0.909  ν=6.25       persistence 0.9782 vs a true 0.98
```

⚠️ **It did not work at first, and the reason generalises.** Optimising raw `omega` alongside
`alpha` and `nu` spreads the parameter vector across six orders of magnitude — ω ~1e-6, α ~0.08,
ν ~6 — which is what SLSQP handles worst. **Unscaled, it returned ω = 24.3 on a series whose
variance is 1e-4.** `omega` is now optimised as a *multiple* of the sample variance so every
parameter is O(1). **The fat-tail test caught it, not the recovery test** — recovery still looked
plausible while ω was nonsense, which is the argument for testing an estimator from more than one
direction.

§2 specifies Student-t *"because GC returns have fat tails"*. That is a claim about the estimator as
well as the data, so it is tested both ways: **ν=4 data fits ν=3.96, Gaussian data fits ν=129.**

## 17. 🔴 §2's GARCH horizon formula is the wrong variance

**The most consequential spec finding of the day.** §2 writes:

```
σ²_{60,GARCH} = σ̄² + (α+β)^m (σ²_t − σ̄²)
```

**That is the variance of the return AT bar t+m** — one 5-minute bar, twelve bars from now.
§Objective's estimand is `|P_{t+60} − P_t|`, whose variance is the **sum** over those twelve bars.
Measured: **the two differ by 13×.**

**The second consequence is worse than the first.** The terminal version also sits on a different
scale from the Yang-Zhang leg, which scales by `√m` — so §2's own `0.5·YZ + 0.5·GARCH` would be
averaging two quantities that are not the same thing, **and the ensemble would look like it worked.**

`garch_forecast` keeps the spec's per-step formula because the recursion needs it; `garch_sigma_h`
aggregates it in closed form, and a test checks that closed form against summing the per-step
forecasts — a mis-derived geometric series produces a number that is wrong and entirely plausible.

**`r_ratio` is trailing, and a test proves it** by appending a 10× spike to a calm series and
asserting the early ratios are unchanged. A full-sample median knows the future, and it is the
single easiest way to fake §2's 1.20 quality filter.

## 18. An audit against the minimal-code rules

| Rule | Result |
| :--- | :--- |
| Files < 300 lines | ❌ `expansion.py` was **442** → split by responsibility into `expansion` 79 / `volatility` 279 / `hurst` 108 |
| Functions < 40 lines | ⚠️ `sweeps` 53, `folds` 42 — but **37 and 26 of actual code**, the rest docstring. **Declined** |
| No dead code | ❌ the split left six orphaned imports → deleted in the same commit |
| Dependencies justified | ✅ scipy for the MLE and `lfilter`; `statistics.NormalDist` had already avoided a second |
| Measure, don't guess | ❌ → the Hurst stride is now measured, 8s → 0.7s |

**The declined one is the point.** The 40-line rule exists for cognitive load and the metric counted
prose; adding a function boundary to satisfy it would add lines without deleting more. **LOC is a
proxy, not a target.** Every existing importer of `expansion.py` — `base_rates`, `families`,
`signals/engine`, `regime_filter`, `portable` — only ever took `atr`, `bar_range` and `body_ratio`,
so the split moved nothing outside the tests. `features/portable.py` is 469 lines and predates this
block; left alone, because unrelated refactoring is not this commit's business.

## 19. Verified against real data, which nothing in this block had been

Every feature had only seen synthetic fixtures. Run over **GC 2026-06 — 2,009,642 trades, 6,024
five-minute bars, price 3971–4576:**

```
yang_zhang  n=12 median 7.98e-04   n=288 median 9.33e-04   100% finite, monotone in n
hurst       dfa 0.519   vt 0.510   agree True
garch       persistence 0.9751   nu 4.61
```

**GARCH's `nu = 4.61` independently confirms §2's stated reason for specifying Student-t.** And
**Hurst reads 0.51–0.52 on real gold, reproducing M1's random-walk finding from a completely
different estimator** — a genuine cross-validation of a result this repo already had.

## 20. 🔴 The S13 seam disagreed with both sides of itself

Nothing had ever produced a frame satisfying `frame.require()`. `pipeline.build` is that assembly,
and the first run proved why it was needed:

```
frame.FEATURES        declared  r_hat_60_bp
fsm.exclusions        read      r_hat_60_usd
volatility.r_hat_60   returned  USD per ounce
```

**Three files, one quantity, three opinions — the precise failure a frozen seam exists to prevent,
sitting inside the frozen seam.** Amended to `r_hat_60_usd`, because every threshold it is compared
against is in USD per ounce: §Objective's `$15.00`, §10's `$2.60 ≤ D ≤ $5.00`, and §10's `$10`
floor and `$15` cap. Carrying it in bp would mean converting at four call sites — four chances to
divide by the wrong price.

**The rule this adds, now in `contracts.md`:** *a seam is not verified by freezing it. It is
verified the first time something produces a frame that satisfies it and something else consumes
one.*

**`NaN` means "not measured", never "zero", and a test enforces it.** `p_build`, `p_expand` and the
three sweep columns carry NaN because E4 has not run and E0 has delivered no ticks. `fsm.exclusions`
reads these by name, and a zero would pass a `>` gate that a NaN correctly fails.

## 21. 🔴 The two Hurst estimators disagree on a rolling window

The frame builds from real GC in **0.2 s**. Two integration findings no synthetic fixture would have
produced — this is the second, and it is the more serious:

```
mean gap  dfa − vt = 0.0849        AGREE_TOL = 0.05
h_agree TRUE on 25.3% of bars
§9 condition 7, H > 0.58:   dfa 10.1%   vt 3.3%      <- they disagree threefold on the gate itself
```

**This is the third distinct symptom of one root cause.** §12 recorded DFA's short-ladder bias; §5
recorded H's scale dependence; this is the same thing reaching the gate §9 actually reads. A
512-bar window can only hold scales 4–128, and **§6's two-estimator agreement rule does not survive
a short ladder.** It is now the strongest argument that **E0 must deliver bars fine enough to
support §6 as written** — the granularity decision flagged in §15 is no longer a nicety.

## 22. ⚠️ A boundary, named rather than crossed

Checking those gates produced pass rates — `R̂ ≥ $15` on **27.5%** of bars, all three gates together
on **0.4%** — which is adjacent to E2's funnel count. **E2 is pre-committed and its prediction is
unwritten, so the measurement stopped there.**

**These numbers are not E2's answer and must not be read as one:** one month, **GC rather than
spot**, with no session, news, sweep, HMM, `p_E` or score gate applied, and **no prediction written
first**. They are a unit check that happened to be informative. Recorded here so that when E2 does
run, it is visible that something adjacent was computed beforehand.

## 23. What changed on disk, third session

```
services/signal-data/pyproject.toml, uv.lock   scipy 1.18.1, scipy-stubs (dev)
services/signal-data/features/volatility.py    new  §2: Yang-Zhang, GARCH-t, the ensemble
services/signal-data/features/hurst.py         new  §6, split out
services/signal-data/features/expansion.py      ~   back to Layer 3 only, 442 -> 79 lines
services/signal-data/pipeline.py               new  the S13 assembly
services/signal-data/features/frame.py          ~   r_hat_60_bp -> r_hat_60_usd
plans/team/contracts.md                         ~   the S13 amendment and its rule
tests/test_pipeline.py                         new  9 tests
```

```
499 passed in 10.96s
All checks passed!
Success: no issues found in 74 source files
```

**337 → 499 tests across the day.** Commits `d0f7d04` · `f8c2019` · `95a9494` · `6f7fa93` ·
`950d186`.

`uv run`'s hang from §14 was **a stale cache, not a project problem** — `uv cache clean` removed
38,267 files (1.7 GiB) and it has been healthy since.

## 24. Where the block stands now

**§2 and §6 are complete and verified on real data.** Built: Tasks 0, 4, 6, 7, 8, 9, 10, 11, 13, 14,
plus the S13 assembly and `spot_s3`'s offline half.

**Three decisions remain, and none of them is work:**

| | Blocks | Whose |
| :--- | :--- | :--- |
| **Four predictions** | E1, E2, E4, and E3's empirical half | Varad ×3, Prathamesh ×1 |
| **An AWS account** | E0, and Tasks 5, 12, 15 behind it | Varad |
| 🔑 **The Anthropic key** | A Week-0 gate line, unmet at four gates | Varad |

**And the granularity decision has hardened from a nicety into a blocker.** Three independent
measurements now say the same thing: §6's 15-minute Hurst cannot be computed from 5-minute bars.
Too few scales (§5), DFA biased high on a short ladder (§12), and the two estimators disagreeing
past tolerance on real data so `h_agree` reads False on three bars in four (§21). **If E0 pulls
5-minute bars, §9's condition 7 is effectively permanently closed and two S13 columns are dead.**

---

# Fourth session — 09:27 to 10:40, Prathamesh's machine

> §15 and §24 above closed with E0 blocked on an AWS account. **Prathamesh created one at 09:00–09:27
> IST** — personal, scoped IAM user, MFA on root — and E0 ran. Three commits.

## 25. E0 — passes, and the cross-check is exact

`aws s3 ls s3://cfg-public-proper-wallaby/XAUUSD/2021/00/ --request-payer requester` answered, and
what it showed resolves §7's *"finding the real object name is E0's first action"*:

```
XAUUSD/2025/05/18/BID_candles_min_1.bi5     the day is a PREFIX holding 1-minute candles
XAUUSD/2025/05/18_ticks.bi5                 the day's ticks are a SIBLING object at month level
```

`day_prefix() + "_ticks.bi5"` — `OBJECT_SUFFIX` as `spot_s3.py` had it "claimed, not measured" —
**was right**. The anchor is midnight: a day object decodes to 228,269 quotes spanning
00:00:00.100–23:59:59.561, and the review's anchor check (§12) stays in as a guard against the bucket
ever switching to hourly objects.

**Cross-check, through `spot_s3.fetch_day` once it existed and not just by hand:** the hour
`SPOT_FEED_CHECK.md` §1 validated over HTTP comes back as **20,654 quotes, median spread 1.91 bp,
mid 3373.862–3392.471. Exact.** The kill condition's second clause does not fire.

**Coverage and cost:** `XAUUSD/` spans **1997-07 to yesterday**, updated daily. 2021–2026 is 1,776
tick day-files, 1.343 GB, zero empty, **~$0.12 at published requester-pays rates** and likely $0
under the free tier. Saturdays have no object (Sundays do), and `fetch_day` returns an empty frame
for them rather than raising — a retry loop must not chase a hole that is supposed to be there.

**§15's granularity worry is closed by the data, not by a decision.** The objects are every quote of
the day, so 1-minute bars and a 15-minute Hurst are both derivable; `h_dfa_15m` is not dead on
arrival. Task 5's loader builds bars from ticks at whatever granularity §6 needs.

**One thing chased and found not to be a bug:** 2023's files are ~40% smaller than 2022's or 2024's.
No missing days. Two comparable Wednesdays decoded — quotes per dollar of range **9,275 vs 9,253**,
spreads 1.62 vs 1.59 bp. Dukascopy quoted 2023 at the same density; gold moved half as much (53 bp
daily range vs 112). **That is a low-magnitude year in `mathematical.md`'s own sense**, and §14's
train/test split must not put all of one regime on one side by accident. Recorded in
`E0_TRANSPORT.md` §2 and `strategy-precommit.md` §13.

⚠️ **Sequence, honestly.** The first probe ran the previous evening, before Task 0 was on `main`,
because its first step surfaced the account decision and that had to exist before 09:00 rather than
be discovered at it. §13 carries no prediction by its own design, so nothing was contaminated; the
order is recorded here rather than tidied.

⚠️ **The access key needs rotating.** It was pasted into a chat transcript before it reached
`~/.aws/credentials`. Prathamesh chose to proceed and rotate afterwards; it is read-only on one
public bucket on a personal account, and it is still the same kind of event as row #0. Delete and
reissue it in the IAM console today, then strike this paragraph.

**Parallel work collided, and was resolved by dropping mine.** Prathamesh's session had committed
Task 0 (seam + §13) and an E0 module locally before seeing this file's nineteen commits. The seam
here landed first and had already been through §20's bug fix, so the local Task 0 was discarded
(`backup/e0-local-2026-09-18` keeps it), `boto3` was re-added on top of `scipy`, and `spot_s3.py`
was **completed rather than replaced** — same `day_prefix`, same anchor check, same symbol guard;
what changed is the docstring's "unverified" became "measured", `boto3` moved to a top-level import
as its own comment asked, and three tests pin what E0 found.

## 26. What changed on disk, fourth session

```
services/signal-data/pyproject.toml, uv.lock       boto3 1.43.97, boto3-stubs[s3] (dev)          own commit
services/signal-data/spot_s3.py                    completed: tick_key, decode_day, NoSuchKey -> empty, boto3 top-level
services/signal-data/tests/test_spot_s3.py         6 -> 9
services/signal-data/analysis/E0_TRANSPORT.md      new — the verdict, the layout, coverage, cost, the 2023 check
plans/team/strategy-precommit.md                   §13 scored
daily_updates/2026-09-18.md                        §25–§26
```

**502 passed** (499 before), `ruff` clean, `mypy` clean across 74 files.

## 27. Where the block stands after E0

E0 was the critical path; it is open. Of the four decisions §15 listed, **one is made** (the AWS
account) and **the `scipy` one was made at 09:17**. Still on a person: four predictions (Varad ×3,
Prathamesh's §15), the Anthropic key, and the key rotation above. **Tasks 5, 8, 12 and 15 are
unblocked** — the spot bar loader reads `spot_s3.fetch_day` and builds bars from ticks.

## 28. The strategy workflow, drawn — and two things the drawing found

**Why this exists.** `docs/PROJECT_MAP.md` landed at 11:00 and answers *what is in this repo*. It
does not answer *what happens to a strategy, from data to verdict*, and the three tracks in this
codebase do not share a pipeline — so a single diagram of "the pipeline" would have been wrong three
ways at once. [`docs/STRATEGY_WORKFLOW.md`](../docs/STRATEGY_WORKFLOW.md) draws them separately.

| Track | Instrument | Question | State |
| :--- | :--- | :--- | :--- |
| **A — order flow** | GC only | Does the aggressor side predict direction? | ⛔ parked, −0.0058 / −0.0003 vs its own null |
| **B — portable** | GC + XAUUSD | Does geometry, vol state or the clock pay? | ✅ finished, negative, no cell clears the floor |
| **C — magnitude expansion** | spot XAUUSD | When is the next hour *large*? | ◐ three tested blocks, not yet one chain |

**How it was built, because it changes what it is worth.** Every arrow was checked against the code
— signatures, module imports, constants — rather than read off prose. That is the whole reason the
two findings below exist; a document written from `PROJECT_MAP.md` and the spec would have drawn
Track C as an end-to-end chain and been wrong.

### 28.1 The frame-row → FSM-row adapter does not exist

`fsm.py` imports exactly two things from this repo: `e3_cost` and `features.frame.TradeCandidate`.
**It does not import `pipeline` and it does not import `classifier`.** It consumes a plain
`row: dict[str, float]`, and the keys it reads are not S13 column names:

```
fsm reads                                  S13 frame supplies
r_hat_60_usd, news_lockout            ✅   same names
h_agree_value                         ✏️   h_agree
has_sweep                             🔧   swept_level, reclaim_dt_s, wick_w
p_e                                   🔧   classifier.apply_isotonic output
d_usd, slippage_usd                   🔧   §10 / e3_cost
s_vol s_sweep s_hurst s_hmm
      s_spread s_news                 🔧   §13 components, none derived anywhere yet
```

Each of the three blocks is real and tested — `pipeline.build` produces a frame `frame.require`
accepts, `classifier` fits and calibrates, `fsm` scores, excludes, admits and sizes. **The chain
between them does not run.** Recorded here rather than discovered in a week, because the failure
mode of not recording it is somebody assuming the pipeline is end-to-end on the grounds that each of
its parts is.

The adapter is small and it is the next thing to write. It is **not** written today: it needs the §7
and §9 columns to be something other than NaN, and §7 is what E4 is for.

### 28.2 E2's stage 13 needs a volume profile spot does not have

`mathematical.md` §9 condition 13 is *"the reclaim occurs near a prior-day LVN or HVN rejection
zone"* — a **volume profile**. Spot XAUUSD has no real volume, only tick counts. §15's dead-stage
list names 8, 9, 10 and 14 (CVD_z, CVD-rising, OFI_z, Hawkes) and **stage 13 is not on it.**

Either it joins them as SKIPPED, or the tick-count proxy is written down as its definition **before**
E2 runs. This is not a solo call — the funnel's stage list is the spec's, and a stage quietly
redefined mid-run is the thing the 16-row attrition table exists to prevent. **Raised, not decided.**

### 28.3 One thing to notice before §15 is signed

Stages 4–6 are the HMM stages and they are NaN today. If they skip or pass through in E2's first
run, **stage 3 is the only magnitude condition doing any work** — and §15's prediction becomes a
prediction about the $15/oz filter, which is E1's question. The two predictions are less independent
than the section numbering suggests, and whoever signs them should sign them in that order and say
so.

## 29. What changed on disk, fifth session

```
docs/STRATEGY_WORKFLOW.md      new, 348 lines — three tracks drawn separately, verified against code
plans/current.md               orientation-documents block, with both findings stated
daily_updates/2026-09-18.md    §28–§29
```

**No code changed**, so no test run is claimed. `PROJECT_MAP.md` and `STRATEGY_WORKFLOW.md` are both
maps: `plans/current.md`, `plans/team/` and the two `ARCHITECTURE.md` files win over either of them.

## 30. The frame-row → FSM-row adapter, written

§28.1 recorded that `fsm.py` imported neither `pipeline` nor `classifier` and that the `row` dict it
consumes used names the S13 frame does not carry. **`fsm.fsm_row` is that wire**, and it lives in
`fsm.py` rather than in a new module: `fsm` is what defines the row's contract, so the contract and
its one constructor sit in the same file. 84 lines, and `fsm.py` is 278 — still inside the 300 the
style rule asks for.

```python
fsm_row(row, *, p_e, d_usd, slippage_usd) -> dict[str, float]
```

`p_e`, `d_usd` and `slippage_usd` are **required keyword arguments, not frame columns** — the
classifier's calibrated output, §10's stop distance, and an execution estimate. A defaulted `p_e` is
a trade admitted on a probability nobody computed, which is the same failure `admit` already refuses
by requiring `ts`, `entry`, `side` and `cost_bp`.

### 30.1 The three decisions inside it, because none of them came from the spec

**§13 gives no normaliser.** It says *"normalize each component to [0,1]"* and supplies no formula
for any of the nine components. The six are anchored on constants the spec **does** give — $15/oz,
30 seconds, and H = 0.5 for a random walk against H = 1 for full persistence — so no tuning knob is
introduced, but **they are a choice, not a derivation, and the room should ratify them.** They are in
one place because every caller inventing its own privately is how two backtests come to disagree
about what `Score ≥ 0.72` means.

**The conservative Hurst.** §9 condition 7 reads one `H_15m`; there are two estimators and they agree
on 25.3% of real GC bars. `h_agree_value` is `min(h_dfa_15m, h_vt_15m)`, so neither estimator alone
can carry a bar past 0.58. `h_agree` stays in the frame for the classifier and is deliberately not
consulted — `min` already refuses everything a boolean gate would.

**One defined zero.** No sweep is an *observation* — §9 looked and there was none — so `has_sweep` is
`0.0` and `exclusions` refuses with `no_sweep`. A missing HMM is an *absent measurement* and raises.
Same distinction as `pipeline.build` writing NaN rather than 0.0.

### 30.2 The bug it closes, which was not the bug it was written for

`fsm_row` raises on any non-finite output value. That was meant as hygiene; it turns out to be the
whole point.

> `score` sums to NaN on a NaN component. **`NaN < SCORE_MIN` is False**, so a NaN score does not
> fail `admit`'s gate — **it passes it**, and `admit` returns a live `TradeCandidate` on a score that
> does not exist.

`test_a_nan_score_would_otherwise_be_admitted` asserts that directly against the FSM, so it fails the
day `admit` stops needing the adapter to protect it. **`admit` itself is still unguarded** — every
path from the frame now goes through `fsm_row`, but a caller that hand-builds a row bypasses it.
Hardening `admit` is one line in Prathamesh's module and was **not** done here.

Because §7's `p_expand` is NaN on every row today, **`fsm_row` raises on every real frame row until
E4 settles whether the HMM is identifiable on five observations.** That is the correct behaviour and
it is the honest state of the block: the seam is closed, and the data to push through it does not
exist yet.

### 30.3 Verification

```
uv run pytest -q          530 passed  (518 before — +12, all in tests/test_fsm.py)
uv run ruff check .       All checks passed
uv run mypy .             Success: no issues found in 76 source files
```

The baseline was measured, not assumed: `git stash`, run, `git stash pop` → 518.

The twelve tests are written on what the seam must **refuse**: a NaN HMM raises and names the
component; a hand-built NaN row is admitted by the FSM (the bug, pinned); no sweep is a refusal and
not an exception; the lower Hurst is the one used, from either order; a disagreeing pair cannot pass
0.58; the spread comes back to USD before being divided by the stop (in bp it would read as a third
of the stop rather than a tenth — a 3000× error that looks reasonable on the page); a spread wider
than the stop is a full penalty and not more; `s_vol` is zero at the spec's own $15 floor and the
exclusion fires at the same number; a zero stop raises rather than reporting no slippage; and the
three non-frame inputs are required rather than defaulted. One test takes a frame row end to end into
a `TradeCandidate`.

## 31. What changed on disk, sixth session

```
services/signal-data/fsm.py            +84  fsm_row, ROW, _unit; imports numpy + structure.MAX_RECLAIM_S
services/signal-data/tests/test_fsm.py +108 twelve tests on what the seam refuses
docs/STRATEGY_WORKFLOW.md              §5.3 and §5.4 rewritten — the wire exists, and what it refuses
daily_updates/2026-09-18.md            §30–§31
```

**Still open, and unchanged by this:** the four predictions, the Anthropic key, E4's HMM verdict, and
E2's stage 13 (§28.2).

## 32. Track C as an independent engine — the plan

**Asked for:** Track C becomes a purely mathematical algo engine that *suggests a trade*, built
independently, with its own filters and backtests, in the shape of `~/Documents/Quant_trading`.
**Written:** [`docs/strategy/TRACK_C_ENGINE.md`](../docs/strategy/TRACK_C_ENGINE.md), 385 lines. It
is a plan and nothing in it is implemented.

**Grounded in the reference, not in memory.** `Quant_trading` was read for this: `backtest.md`'s
G1–G8 gate contract and §3 fill model, `src/backtest/` (engine, `fill_model`, `walk_forward`,
`metrics`, `report`), `src/strategies/base_strategy.py`, `src/risk/` (16-step engine with veto), and
the README's strategy stack.

### 32.1 The tension the plan opens with

The two codebases have opposite reflexes. This one ends pipelines in a **table** — `reach.py` is *a
lookup, not a chooser* — and Quant_trading ends them in an **order**. Track A finished flat on its
own null and Track B finished with no cell clearing the cost floor. **An engine that suggests trades,
built by the same people on the same data, will produce a backtest that looks good long before it
produces one that is true.** So the plan takes Quant_trading's structure and keeps this repo's burden
of proof: build the engine, and do not believe it until it clears out-of-sample gates with a deflated
Sharpe and a pre-committed prediction standing against it.

### 32.2 What it specifies

- **Independent in control flow and measurement, not in arithmetic.** `services/signal-data/track_c/`,
  ten files, ~1,100 lines. It imports every feature definition — `features/*`, `fsm`, `classifier`,
  `e3_cost`, `tuning` — rather than forking nine tested modules, because rule 10 is one definition
  per quantity. It does **not** share `backtest.py`: that is a vectorised grid sweep and Track C is
  event-driven with a 20-second order life. The one thing that must not fork is the first-touch
  convention, which is imported.
- **Four filter layers that can only refuse, never negotiate** — admissibility (`fsm_row`), the
  16-condition funnel, score + exclusions, then risk. A run emits a **refusal histogram**, because
  the first honest artefact this engine can produce is an attrition table and not an equity curve.
- **Its own backtest**, with Quant_trading's pessimistic fill model: cross the spread, 1.5×
  slippage, last in queue, no positive slippage on limits, gappy stops punished.
- **Eight gates committed before the first run.** Six adapted from Quant_trading, one added
  (deflated Sharpe, which that repo lacks), **two dropped with reasons** — daily win rate and
  ≥60 trades/year are meaningless at roughly one trade a week, and the second would reject the
  strategy for exactly the thing E2 is designed to measure.
- **A build order in which roughly 400 lines are unblocked today** (`config.py`, `levels.py`,
  `fills.py`, `metrics.py`) and testable on fixtures before a single spot bar is pulled.

### 32.3 Three things the plan found on the way

**A real defect.** §10 requires `20 ≤ D ≤ 50` ticks — $2.00 to $5.00/oz — and `fsm.exclusions`
enforces **only the upper bound**. A $0.50 stop passes today and its target becomes
`clip(2.5 × 0.50, 10, 15) = $10`, a 20:1 nominal R that §10 exists to forbid. Fix is a
`stop_too_tight` exclusion at `D_MIN_USD = 2.00` — one line and one test, in Prathamesh's module.
**Not made here.**

**§9's cancel conditions may not survive E4.** Four of the five reference OFI, CVD or Hawkes and
cannot run on spot. The fifth is *"the estimated expansion probability falls below 0.55"* and depends
on the HMM. **If E4 kills the HMM, the limit order has no cancel condition at all** beyond its
20-second expiry, and whether that is acceptable is a decision nobody has made.

**5-minute bars cannot resolve a 20-second order life.** §9's event is a 30-second sweep and a
20-second limit. Intra-bar fills are an assumption, not a measurement, and the engine's first results
are an **upper bound** until Track C gets its own version of `replay.py`'s intra-bar check. That is
step 8 of the build order rather than a footnote.

## 33. What changed on disk, seventh session

```
docs/strategy/TRACK_C_ENGINE.md   new, 385 lines — the independent-engine plan
daily_updates/2026-09-18.md       §32–§33
plans/current.md                  pointer to the plan
```

**No code changed**, so no test run is claimed. `3d761fb` (the `fsm_row` adapter) is pushed to `main`.

## 34. §10's missing stop floor, closed

§32.3 recorded it and left it: **§10 reads `20 ≤ D ≤ 50` ticks and `fsm.exclusions` enforced only the
upper bound.** Now both.

```python
D_MIN_USD = 2.00              # §10, 20 ticks
D_MAX_USD = 5.00              # §10, 50 ticks
```

**Why the floor is not just symmetry with the cap.** The target is `clip(2.5·D, $10, $15)`, so a
$0.50 stop bought a $10 target — **a 20:1 nominal R produced by the target FLOOR rather than by
anything anyone chose.** §10's own note says the backtest must distinguish *target distance in ticks*
from *risk-reward multiple*; refusing the stop is how that distinction gets **enforced** rather than
merely reported afterwards.

**Where the numbers come from.** 20 and 50 ticks at GC's $0.10 — the same translation
`TARGET_FLOOR_USD` and `TARGET_CAP_USD` already carry for §10's 100 and 150. **XAUUSD's `tick` is
Dukascopy's 0.001 price quantum, explicitly not a tradeable tick**, and `instruments.py` says so at
length; it is not where these come from, and a test pins that.

Four tests: both bounds fire; the constants are §10's ticks in USD; **`D == $2.00` itself is allowed**
(§10 reads `20 ≤ D`, so the floor is inside the spec and not the first value out); and the 20:1
target that motivated the whole thing is now refused at `admit`.

```
uv run pytest -q          534 passed  (530 before)
uv run ruff check .       All checks passed
uv run mypy .             Success: no issues found in 76 source files
```

## 35. What changed on disk, eighth session

```
services/signal-data/fsm.py            D_MIN_USD, stop_too_tight
services/signal-data/tests/test_fsm.py +4, a §10 section
docs/strategy/TRACK_C_ENGINE.md        §2.1 marked fixed; build-order step 2 struck
plans/current.md                       the pointer updated
daily_updates/2026-09-18.md            §34–§35
```
