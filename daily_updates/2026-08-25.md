# 2026-08-25 — Day 3

Plan item #4: replace the lexicon with a real model. Owner Varad. Done as a drop-in —
the API contract did not move, so nothing outside `services/api` changed. Shipped as
[PR #3](https://github.com/Prathameshh-Patil/trading-intelligence/pull/3), open for review.

A2 (spot XAUUSD correlation) was deliberately not started: it depends on Prathamesh's
resampled GC series, and `services/signal-data/` still has no `data/` directory, so plan
item #6 has not been run. Nothing to correlate against yet.

---

## What changed

### `app/analysis.py` — rewritten

The Day 1 lexicon is deleted. `analyze(text)` now makes one `client.messages.parse` call
to `claude-haiku-4-5` with a system prompt that defines each of the four fields, and
decodes the reply through a Pydantic `Analysis` model via structured outputs.

Structured outputs rather than forced tool use: same guarantee that the shape is
enforced, but no tool-call round trip and no manual `json.loads`, so it is meaningfully
less code for an identical result. `parsed_output` is `Analysis | None` — `None` means
the model refused or ran out of output tokens mid-object, so that raises rather than
returning a half-answer.

The four keys are unchanged. That was the whole design of the Day 1 baseline and it held:
no extension change, no contract change, no migration.

### `app/api/routes/analyze.py` — a `503` path

`anthropic.APIError` (service down, rate limit, bad key) and a schema-failing answer both
become `503 {"status": "error", "analysis": "unavailable", "error": "..."}`. This mirrors
the shape `/api/v1/health/db` already returns, so the API has one way of saying "the thing
behind me is not answering" rather than two.

`ValidationError` subclasses `ValueError`, so one `except (anthropic.APIError, ValueError)`
covers both. The function it wraps is five lines, so the catch is narrow in practice.

Checked the extension before adding this: `apps/extension/src/lib/api.ts` renders any
non-200 as `The analysis service returned ${status}` and never reads `detail`, so a
dict-shaped detail cannot break it. No extension change needed.

### `app/config.py`, `.env.example` (both) — the key

`anthropic_api_key: str`, required, no default — identical treatment to `database_url`.
A missing key is a startup failure with a clear pydantic error, not a surprise at the
first request. Documented in `services/api/.env.example` and the repo README, including
the point that tests only need the key to be *present*, not valid.

### `tests/test_api.py` — restructured

The old `test_bullish_text` / `test_bearish_text` / `test_neutral_text` /
`test_negation_flips_sentiment` asserted real lexicon behaviour. Against a network call
they would be slow, flaky and billable; against a stub they would be asserting that a stub
returns what the stub was given. Neither is worth having, so they were split:

- **13 boundary tests** stub `run_analysis` via `monkeypatch` and cover what actually
  breaks silently: response shape, whitespace stripping, the three validation rejections,
  the `503`, and CORS across Chrome/Firefox/localhost plus a foreign origin.
- **5 live tests** (bullish, bearish, neutral, negation, confidence/signal bounds) make
  real calls and skip unless `LIVE_API_TESTS=1`. A normal `uv run pytest` spends nothing.

Four more were added later in the day — see the verification section, which is where the
weakness in this split showed up: stubbing `run_analysis` meant `analysis.py` itself was
never executed by any test. Final count 17 (+5 skipped).

### Dependency

`anthropic>=1.0.0` (resolved 1.0.0). One dependency, justified in a sentence: it is the
official client for the service the analysis now runs on. `messages.parse` and its
`output_format` parameter were verified against the installed version rather than assumed.

---

## Going back to verify — and the defect it caught

Went back to actually verify rather than trust the green suite, by driving `analyze()`
against a faked Anthropic transport (`httpx2.MockTransport`) so the real request building
and decoding ran. Four cases; two of them failed in a way that mattered.

**The defect.** `Analysis` was validating the *system prompt's style* as if it were the
contract: `confidence` bounded to 50–95, `signals` to at most 4. Dumping the schema the SDK
actually sends showed why that is wrong — numeric bounds and list maximums are stripped and
reappear only as a description string:

```
"confidence": {"type": "number", "description": "{maximum: 95, minimum: 50}"}
```

So the model is *asked* for 50–95, not constrained to it. And because `messages.parse`
validates client-side on the way back, any bound placed there becomes a hard error rather
than a correction. A model answering `confidence: 97` — a perfectly usable analysis —
raised `ValidationError`, which my own route then dutifully turned into a `503 analysis
unavailable`. Same for a 5th signal. Two spurious user-facing failure modes, both invented
by me, neither visible to `ruff`, `mypy`, or 13 passing tests.

Fixed by writing `Analysis` to the contract rather than the prompt: `confidence` 0–100,
`signals` non-empty. The prompt still asks for 50–95 and 1–4. Genuinely unusable output
(truncated JSON, refusal) still fails, which is correct.

| Model returns | Before | After |
| :--- | :--- | :--- |
| well-formed reply | 200 | 200 |
| `confidence: 97` | **503** | 200 |
| 6 signals | **503** | 200 |
| truncated JSON | 503 | 503 |

This is the second real defect verification caught, after the mypy `None` one. Both were in
code that type-checked and tested clean.

**The failure path is now verified against the real API.** A live call with the placeholder
key reached `api.anthropic.com` and came back `AuthenticationError` (401) — an `APIError`
subclass, so the route's `503` mapping is confirmed against the real service rather than a
mock of it. Costs nothing, since a rejected request is not billed.

## Final state of the checks

Run from `services/api`, Postgres up:

```
uv run ruff check app tests   # All checks passed!
uv run mypy app               # Success: no issues found in 10 source files
uv run pytest -q              # 17 passed, 5 skipped in 1.06s
```

Four of the 17 swap the Anthropic transport instead of stubbing `analyze`, so
`app/analysis.py` is genuinely exercised: what goes over the wire, decoding a well-formed
reply, style drift still returning 200 (the regression guard for the defect above), and a
truncated reply becoming a 503. The original 13 stubbed `run_analysis` outright, which left
`analysis.py` with no coverage at all — that was the gap that hid the defect.

mypy caught one real defect during the work — `parsed_output` is `Analysis | None` and the
first draft called `.model_dump()` on it unguarded. That is the `None` check described
above; it was a genuine crash path, not a type-checker complaint worked around.

## Live attempt — key created, blocked on billing

A real API key was created and added. The 5 live tests still fail, but the reason moved:

```
503 -> 400 invalid_request_error
"Your credit balance is too low to access the Anthropic API."
```

That is progress worth recording precisely. The key **authenticates**, and the request gets
past auth and schema validation all the way to the billing check — a malformed request would
have been rejected earlier with a validation message instead. The upstream `400` is caught
and surfaced as a `503` with the real reason readable in `detail.error`, which is the error
handling working correctly on a case it had not seen. Nothing was charged.

So the code is not what is blocking any more. The backend choice is.

**Near-miss worth writing down.** The key was first added to `.env.example` rather than
`.env`. `.env.example` is *tracked*, and it was already staged — a `git commit -a` would have
committed a live key. Caught before any commit; `git log -S` confirmed no commit in any branch
contained it, and it was never pushed, so there was no exposure and no rotation needed. The
key was moved to `.env`, the placeholder restored, and the index re-checked clean. The two
files sit side by side with near-identical contents, which is exactly why this happened.
Added to the standing constraints in `plans/current.md`.

## Backend decision — deliberately deferred

Rather than defaulting to "just add credit", the options were laid out and the decision was
consciously postponed:

| Option | Cost | Trade |
| :--- | :--- | :--- |
| Anthropic credit | ~$5 ≈ 3,000 analyses | Zero further work; code stays exactly as verified |
| Ollama, local | Free forever | No key, no rate limit, page text never leaves the machine — but cannot back a deployed API |
| Gemini free tier | Free, no card | Closest drop-in; rate-limited, and its free-tier data-use terms need reading first |

This is the same question as plan item #4 ("where does the API live") and should be decided
with it: a local model removes the secret and rules out a hosted API; a cloud model does the
reverse. Deciding them separately would mean deciding one of them twice.

Whichever wins, `analyze()` keeps returning the same four keys, so the route, the contract
and the extension are untouched. Day 1's decision to freeze that contract is still paying.

## What is NOT verified

**No successful analysis has ever been produced.** First there was no key (401), then there
was a valid key on an account with no credit (400). A real request reached the API both times
and the error path handled both correctly — but the success path has never once executed.
Concretely, all of the following are unknown:

- whether the model returns sentiment that matches the four cases the lexicon used to pass
  (this is the whole point of the change, and it is the one thing still untested)
- real latency, and whether the popup's UX holds up with a network call in the path
- real cost per request (the ~$0.0015 figure is arithmetic on published Haiku 4.5 rates —
  $1/MTok in, $5/MTok out — not a measurement)

The `output_format` question that was open here has since been answered — see the defect
above. Those bounds are enforced by neither the API nor the decoder; only the prompt asks.

17 tests, `ruff` and `mypy` passing is evidence that the wiring, schema, decoding and error
handling are correct — that is more than was established on the first pass, and it now
includes the real 401. It is still not evidence that the analysis *works*. Those are
different claims and only the first has been earned.

To close it: settle the backend question above, then

```
LIVE_API_TESTS=1 uv run pytest
```

and click one real selection through the loaded extension. That is plan item #1.

---

## Shipped

Branch `feat/claude-analysis`, commit `8b7eb55`, opened as
[PR #3](https://github.com/Prathameshh-Patil/trading-intelligence/pull/3) against `main`.
11 files, +744/−142.

The PR body leads with the change but gives equal space to the two defects and to what the
work does *not* establish — that no successful analysis has ever run, and that the backend
choice is open rather than defaulted. Prathamesh hits the required-key change first, so
there is a note for him that a placeholder is enough to run the suite; only the 5 live tests
need a valid key.

Re-scanned the pushed diff for key material before and after pushing: clean. `.env` is
gitignored and never left the machine.

**Dependency is its own commit**, per the rule in the repo README that dependency changes
get their own reviewed commit with the lockfile diff. It was initially folded in with the
code — the reasoning being that `anthropic` is meaningless without the code that imports it,
so an isolated commit adds an unused package — and then split back out, because the point of
the rule is that a 112-line `uv.lock` diff is reviewable on its own and invisible when
buried under a feature. It goes first, so the code commit that imports it is never broken.

The branch is therefore three commits: dependency, code, docs.

---

## Notes for tomorrow

- Plan item #4 (**"decide where the API lives"**) grew a second dimension. The API now
  holds a secret, so it cannot go anywhere that cannot hold an env var, and the key must
  never move into the extension — the extension is public, and anyone who installs it can
  read anything shipped inside it.
- Nothing rate-limits or caches `/api/v1/analyze`. It was free to call when it was a
  lexicon; it is not free now — and on a free tier it would be rate-limited rather than
  merely costly. Worth a thought before anything loops over it, under any backend.
- The system prompt in `app/analysis.py` is now the only thing holding sentiment behaviour
  in place. No type checker and none of the 17 default tests will catch a regression in it,
  which is exactly what the 5 live tests are for.
