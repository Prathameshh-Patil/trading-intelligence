# 2026-09-02 — three of the four Friday prep items, done three days into the gap

Follows [`2026-08-28.md`](2026-08-28.md), the Week 0 gate day. **The gate did not happen** — this is
not a correction of that, just a note that today's work sits inside `week-01.md`'s stated gap
(29 Aug – 4 Sep, "nobody works"), not inside Week 1 itself. Asked to check whether Week 0 was
actually closed before starting Week 1, the honest answer was no: `contracts.md`'s S1 `'N'`
amendment is still an unvoted proposal, the Anthropic key from `current.md` #0 is still live,
`thresholds_selector.md` Part B is still empty, and nobody has checked Firefox, screenshotted the
Tauri window, sent the three vendor emails, or booked the CA call. **None of that changed today.**
What changed is three of the four items on `week-01.md` §1's Friday prep list — the ones that are
local, reversible, and need no account, no vote, and no trading judgment.

## Regenerated the regime labels

`analysis/regimes/` was stale for two independent reasons: a five-feature matrix `regimes.py` no
longer has (`cvd_efficiency_specced` correlated with `cvd_persistence` at r=0.803 and was dropped),
and the straddling-window bug `d3c896f` fixed. `week-01.md` calls regenerating it "the hard blocker"
for Varad's Day 1 — his whole first day reads that CSV.

Re-ran the exact command in `analysis/regimes/README.md`, same month (`gc_trades.parquet`, July 2026,
1,616,772 trades), same `k=3`, into a new directory rather than over the old one, per §6.2's rule that
a pre-commitment is a record, not something to rewrite:

```
uv run python regimes.py --parquet data/gc_trades.parquet \
    --bar-size 5min --window 12 --vol-window-min 30 \
    --level window --k 3 --split-date 2026-07-19 \
    --no-session-phase-in-clustering --out-dir analysis/regimes_2026-09-02
```

| | Old (26 Aug) | New (today) |
| :--- | ---: | ---: |
| Features | 5 | 4 |
| `n_fit` | 3,493 | 3,373 |
| `n_labelled` | 6,243 | 6,023 |

Regime split: **15.4% / 21.5% / 63.0%** across 3 regimes, silhouette 0.297 (k=3 beats k=2's 0.239).
Median run 7 bars (35 min), longest 68, regime changes on 9.5% of bars at `dwell_lambda=0.5`.

**Not done: a second pair of eyes on the plot.** §6.2 asks for a human review before anything selects
against these labels, and whether this run promotes over the stale one is a call for the room, not
for whoever ran the regeneration.

## Exported the fixture to JSON

`engine/mock.ts` (W1D2) replays the S1 fixture at 10x and needs to read it from TypeScript, which
can't open parquet without a native binding not worth adding for one fixture. Wrote
`export_fixture_json.py` and ran it: `apps/desktop/public/fixtures/gc_ticks_1session.json`, all
77,532 ticks, 4.8 MB, `public/` rather than an import so it's `fetch()`'d rather than bundled into
the JS chunk.

**One decision left alone on purpose:** `aggressor_side` is exported as the raw `'B'/'A'/'N'`, not
squeezed into S2's `Side` type (`'bid' | 'ask'`, no third state). That mapping — what a replay does
with an `'N'` tick given the S1 amendment isn't even voted on yet — is mock.ts's decision on Day 2,
not this export's.

## Fresh-clone check, re-run

Scratch-cloned the repo (never `git clean -xdf` in the working copy — that deletes the only copy of
`gc_trades.parquet` on any machine), then `pnpm install` → `pnpm build:all` (extension, desktop, web
all clean) → `cargo build` (`apps/desktop/src-tauri`, clean, 38s cold). Also ran the Python suite
while in there: **72/72 pass.** Nothing regressed since the 28 Aug check.

## `FeedCreds`, checked not changed

`week-01.md`'s joint 30-minute task says to define `FeedCreds` in `types.ts`. It already is —
`10ed7c5` (25 Aug) added `{ vendor: string; [field: string]: string | undefined }` right below a
comment block that says the field is "deliberately left open." First read that as a contradiction;
it isn't — "open" means no vendor-specific fields are pinned while the vendor (C1, Databento →
quantfeed) is still unsettled, not that the type doesn't exist. No edit made.

## What this doesn't close

- [ ] The S1 `'N'` amendment — still needs Prathamesh, Varad and Shreyas in the same room
- [ ] The Anthropic key — still live, still Varad's, at console.anthropic.com
- [ ] `thresholds_selector.md` Part B — still empty. A prior Claude session already declined to fill
      it in, on the grounds that a threshold picked by an assistant isn't a commitment by the person
      carrying the bias. Same call today.
- [ ] Firefox, the Tauri screenshot, the three vendor emails, the CA call — all need a human at a
      device or on a call, none of which happened here
